"""
Placeholder exactness test. Replace with real tests as processes are implemented.
Each test here must cite the formula being verified (theorem + reference).
"""

import pytest


@pytest.mark.exactness
def test_exactness_suite_is_live() -> None:
    """
    Verifies the exactness suite runs. Not a mathematical test.
    Remove this once real exactness tests exist.
    """
    assert True
